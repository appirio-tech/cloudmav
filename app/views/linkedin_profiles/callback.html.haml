= content_for :title do
  Add your jobs from LinkedIn
  
%h1 Your jobs on LinkedIn

%p We found these jobs on your LinkedIn Profile. 
  
= form_tag confirm_profile_linkedin_profile_path(@profile, @linkedin_profile), :method => :post do
  = big_primary_button_submit_tag "Add my LinkedIn Jobs to CodeMav", :disable_with => "Adding..."

%ul.list
  - @linkedin_profile.linkedin_positions.each do |p|
    %li
      %h3= p.title
      %p= p.company_name
      %p= p.description

