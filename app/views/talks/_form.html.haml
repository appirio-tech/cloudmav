= semantic_form_for [@profile, @talk] do |f|
  = f.inputs do
    = f.input :title
    = f.input :description
    = f.input :links_block, :as => :text
  = f.buttons do
    = big_primary_button_submit_tag "Save"

