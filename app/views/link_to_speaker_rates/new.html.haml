= content_for :title do
  Link your SpeakerRate rating

.grid_16
  %h1 Link your SpeakerRate rating
  = form_tag(:controller => "link_to_speaker_rates", :action => "create", :method => "put") do
    = hidden_field_tag :profile_id, @profile.username
    = label_tag "speaker_rate_talk", "Your talks from SpeakerRate"
    %br    
    = select("speaker_rate_talk", "talk_id", @speaker_rate_talks.map{|t| [t.title, t.id] })
    = big_primary_button_submit_tag "Link SpeakerRate rating"
  %br
  %br
  %p 
    If you can't find the talk you want to link, then you probably haven't added the talk to SpeakerRate.
    %br
    = big_button_link_to "Add talk to SpeakerRate", "http://speakerrate.com/talks/new", :target => "_blank"
  %p
    After the talk is added to SpeakerRate be sure to come back here and sync with SpeakerRate.
    %br
    = big_button_link_to "Sync with SpeakerRate", refresh_profile_talk_link_to_speaker_rate_path(@profile, @talk)

.clear
